#include <stdio.h>

/**
 * main - This is the entry point of the code
 *
 * Return: (0)
 *
 **/
int main(void)
{
	int i = 97;

	while (i <= 122)
	if (i != 113 && i != 101)
	i++;
	{
	putchar(i);
	}
	putchar('\n');
	return (0);

}
